Финальный антиспам-чек-лист (Камчатка)
0️⃣ Базовые принципы (фиксируем)

❌ CAPTCHA / SMS / внешние антиспам-API — не используем

✅ Все решения принимаются на сервере

✅ JavaScript — только для фиксации времени

✅ Любой одиночный сигнал = НЕ бан, только score

1️⃣ Honeypot (обязателен)

Реализация

Скрытое поле (display:none + visibility:hidden)

Правдоподобное имя поля (не honeypot)

Добавляется ко всем формам

Логика

honeypot заполнен → ❌ заявка отклоняется молча

2️⃣ Одноразовый server-token формы (ключевой слой)

Реализация

Генерируется сервером при рендере формы

Передаётся в скрытом поле

Привязан к:

сессии / IP

TTL (10–20 минут)

После отправки инвалидируется

Логика

нет токена / токен просрочен / повторное использование
→ ❌ отклонение без уведомления

3️⃣ Time-based защита (мягкая)

Реализация

JS фиксирует момент появления формы

Значение передаётся на сервер

Логика

< 4–5 сек → +1 к spam-score

не использовать как единственный блок

4️⃣ Антидубликат по телефону (ключевой бизнес-слой)

Реализация

Нормализация телефона (+7XXXXXXXXXX)

Общая проверка по всем формам

Логика

телефон уже есть →

❌ новый лид НЕ создаётся

✅ показать сообщение:

«Вы уже отправляли заявку. Мы скоро свяжемся с вами…»

5️⃣ IP + телефон (жёсткий дубль)

Логика

IP + телефон совпадают

интервал < 60 сек
→ ❌ отклонение без создания заявки

6️⃣ Rate limiting (Камчатка)

Лимиты

1 заявка / 3 мин / IP

2 заявки / 30 мин / IP

Логика

превышение → временная блокировка

7️⃣ DEF-коды (мягкий региональный сигнал)
❓ Нужно ли готовить список камчатских DEF?

ДА — обязательно.

Но:

это белый список, а не чёрный

всё, что НЕ в списке → зона риска, не блок

Реализация

Локальная таблица / конфиг:

kamchatka_def:
  - 415
  - 4152
  - ...


Логика

DEF ∈ Камчатка → 0

DEF ∉ Камчатка → +1 к spam-score

❌ Никогда не блокировать только по DEF

8️⃣ Подозрительные IP-диапазоны (128.70 / 128.71)

Логика

IP ∈ диапазон →

либо лимит 1 заявка / 12–24 часа

либо усиленный spam-score

⚠️ Лучше ограничение, чем полный бан

9️⃣ Server-side fingerprint (без JS)

Использовать

User-Agent (нормализованный)

Accept-Language

IP /24

Логика

одинаковый fingerprint + разные телефоны
→ усиление ограничений

🔟 Серверная валидация (обязательная)

User-Agent обязателен

Имя:

2–100 символов

URL → ❌ блок

Email:

кириллица → ❌ блок

Сообщение:

ограничение длины

📌 Итог по DEF-кодам
✔ Что нужно сделать

Подготовить список “камчатских” DEF-кодов

Использовать его ТОЛЬКО как белый список

Всё остальное → мягкий риск-фактор

❌ Что не делать

не делать чёрные списки DEF

не блокировать заявки только по региону номера