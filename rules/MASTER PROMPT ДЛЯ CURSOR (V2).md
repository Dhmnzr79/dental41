📌 MASTER PROMPT ДЛЯ CURSOR (V2) 0) Контекст проекта и правила

Ты работаешь над редизайном WordPress-сайта.
Старая тема и стили — не используются, это только источник контента.

Опоры проекта:

Архитектура и правила фронтенда — из cursor_frontend_rules.md (БЭМ, разделение CSS, контекст ≤ 1 уровень, запреты)

cursor_frontend_rules

Сетка 12 колонок — из wp_12col_structure.md (.v2-container → .v2-row → .v2-col-\*, брейки 768 / 1280 / 1400, gap 30px)

wp_12col_structure

Паттерны сетки — из grid_faq.md (генерация .v2-row / .v2-col-\*)

grid_faq

Жёсткие запреты:

❌ Тексты НЕЛЬЗЯ менять (ни слова, ни порядок, ни перефраз).

❌ НЕЛЬЗЯ использовать старые классы или их производные.

❌ НЕЛЬЗЯ делать свои grid-сетки (Flex, custom grid и т.п.).

❌ НЕЛЬЗЯ использовать id в стилях и !important.

Обязательное:

✅ Только .v2-container → .v2-row → .v2-col-\*

✅ Только БЭМ с префиксом v2-

✅ CSS строго разнесён по файлам (в папке assets/css/v2/)

✅ Контекст селекторов ≤ 1 уровня

✅ Семантические теги + SEO-обвязка без изменения текста

✅ Все стили обернуты в .v2-site для изоляции

1. Общая логика работы

Мы работаем строго по одному блоку за раз.
Каждый блок проходит 4 этапа:

Чистый семантический HTML (без классов старой темы)

HTML по сетке + БЭМ

SEO-обвязка (без изменения текста)

CSS по файлам

2. ЭТАП 1 — Генерация чистого HTML по сетке
   ВАЖНО: Тексты НЕ ТРОГАТЬ.
   Ни слова, ни порядка, ни структуры фраз.

Используй правила из cursor_frontend_rules.md
Используй сетку из wp_12col_structure.md

Задача: переработать HTML блока: [НАЗВАНИЕ БЛОКА, напр.: HERO]

Требования:

1. Структура: section → .v2-container → .v2-row → .v2-col-\*
2. Схема сетки: [укажи: 8/4, 6/6, 12/12 и т.п.]
3. На мобилке — все колонки 12/12
4. Никаких своих grid/flex систем
5. Все классы: БЭМ с префиксом v2-
6. Никаких старых классов
7. Только HTML, без CSS

Разрешено:

- менять div на section / article / aside
- менять span на h2/h3
- оборачивать элементы ТОЛЬКО при необходимости

Запрещено:

- трогать тексты
- переписывать формулировки

Вот контент:
[ВСТАВИТЬ ОЧИЩЕННЫЙ HTML БЕЗ КЛАССОВ]

3. ЭТАП 2 — БЭМ логика и нормализация
   Проанализируй полученный HTML.

Задача:

1. Спроектировать логичную БЭМ-структуру:
    - v2-[block]
    - v2-[block]\_\_element
    - v2-[block]--modifier
2. Один элемент = один базовый класс
3. Без дублирующих ролей и пустых обёрток
4. Без deep-контекста

Тексты не менять.

Выведи обновлённый HTML.

4. ЭТАП 3 — SEO (без трогания текста)
   Важно: ТЕКСТ НЕ ИЗМЕНЯТЬ.

Задача: улучшить SEO через структуру и атрибуты.

Добавить:

1. Семантические теги: header, section, article, aside, nav
2. Правильную иерархию: h1 → h2 → h3
3. Schema.org микроразметку (MedicalBusiness / Organization / FAQ, если уместно)
4. alt / title / aria-атрибуты для изображений и кнопок
5. itemprop / itemscope / itemtype — БЕЗ изменения текста

**КРИТИЧНО для ItemList:**

- `position` — ВСЕГДА число через `<meta itemprop="position" content="1">`
- Визуальный номер ("01", "02") — отдельный элемент БЕЗ микроразметки
- `name` — на текстовом элементе с контентом
- Структура: `<div itemscope itemtype="ListItem" itemprop="itemListElement"><meta itemprop="position" content="1"><p itemprop="name">...</p></div>`

Никакой переспамки.
Вывести только HTML.

5. ЭТАП 4 — CSS верстка
   Используй строго архитектуру из cursor_frontend_rules.md.

Задача: написать стили для блока v2-[название].

Требования:

1. layout.css — только сетка, отступы, структура
2. components.css — только стили блока
3. utilities.css — только атомарные утилиты, если необходимо
4. НЕЛЬЗЯ:
    - использовать !important
    - использовать id
    - делать контекст глубже 1 уровня
    - переопределять h1/h2/h3 (они в base.css)

Типографика:

- Заголовки — только через теги
- Не дублировать правила из base.css

Адаптив:

- Только по факту ломания
- Брейки: 768 / 1280 / 1400
- Media-запросы внизу файла.

Вывести только CSS.

6. ЭТАП 5 — Оптимизация и чистка
   Проанализируй CSS блока v2-[название].

Задача:
— найти повторы
— упростить
— минимизировать количество классов
— оставить БЭМ
— не нарушать мои правила

Выведи:

1. Оптимизированный CSS
2. Короткий список изменений
