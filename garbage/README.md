# Папка Garbage - Старые файлы темы

Эта папка содержит все старые файлы темы, которые были заменены на версию v2 или больше не используются.

## 📁 Структура

```
garbage/
├── templates/          # Старые шаблоны
│   ├── templates-v1/   # Старые шаблоны страниц v1
│   └── templates-legacy/ # Legacy версии шаблонов
├── pages/             # Старые страницы-обертки (wrappers)
├── styles/            # Старые стили
├── scripts/           # Старые скрипты
└── README.md          # Этот файл
```

## 📦 Перемещенные файлы

### Шаблоны (templates/)
- `templates-v1/` - Полная папка со старыми шаблонами страниц:
  - page-blog.php (заменен на v2)
  - page-clinic-info.php (заменен на v2)
  - page-contacts.php
  - page-implantatsiya.php
  - page-istoriya-moei-ulybki.php (заменен на v2)
  - page-sovety-kitay.php
  - page-thank-you.php
- `templates-legacy/` - Legacy версии:
  - front-page.php (заменен на v2 в корне)

### Страницы (pages/)
- `page-contacts.php` - Wrapper для старого шаблона
- `page-implantatsiya.php` - Wrapper для старого шаблона
- `page-thank-you.php` - Wrapper для старого шаблона

### Стили (styles/)
- `overrides.css` - Переопределения старых стилей
- `popup.css` - Стили попапов (старая версия)

### Скрипты (scripts/)
- `scroll-animations.js` - Старые анимации при прокрутке
- `phone-mask.js` - Маска телефона (старая версия)
- `popup.js` - Скрипт попапов (старая версия)

## ⚠️ Важно

**Файлы, которые НЕ перемещены (могут использоваться):**

- `header.php` и `footer.php` - Оставлены, так как используются стандартными шаблонами WordPress (single.php, page.php, index.php, archive.php)
- `style.css` - Обязательный файл темы WordPress
- `index.php`, `single.php`, `page.php`, `archive.php` - Стандартные шаблоны WordPress

## 🔄 Что нужно сделать дальше

1. Если все страницы переведены на v2, можно также переместить `header.php` и `footer.php` в garbage
2. Стандартные шаблоны WordPress (index.php, single.php, etc.) лучше оставить для совместимости

## 📝 Примечания

Все ссылки на эти файлы были удалены из `functions.php`. Если нужно что-то восстановить - файлы находятся здесь.



















